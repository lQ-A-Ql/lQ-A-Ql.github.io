# 开发记录

## 2026-05-27 11:20 +08:00 | Codex

- 调整首页首屏层次，保留原有粉紫二次元背景风格，新增半透明信息容器、粒子点缀和更明确的主副标题层级。
- 重做顶部导航的玻璃感样式与移动端菜单，使桌面端和移动端入口风格统一。
- 统一首页文章卡片结构，增强分类、标题、摘要、元信息和悬浮反馈的一致性。
- 重组关于区域与页脚，使页面结尾从普通说明块调整为更完整的作者信息与快速入口区域。
- 评审结论：当前仓库缺少稳定的本地依赖链接，`pnpm install` 在本机环境中未完成链接阶段，导致未能在本地完成构建验证；本轮先推远端以依赖 GitHub Actions 的标准环境完成部署校验。

## 2026-05-27 12:12 +08:00 | Codex

- 将首页首屏从大面积毛玻璃面板调整为分层透明液态玻璃结构，减少中心遮挡，让背景人物重新成为主视觉。
- 新增全局 `liquid-glass` / `liquid-glass-strong` / `liquid-outline` 样式，用高光、折射感和轻量内阴影替代传统毛玻璃雾化。
- 降低 hero 总遮罩强度并延长背景图可见区域，同时下移内容区的完全变黑节点，让背景图能更多延伸到首屏底部。
- 顶部导航和首屏按钮同步切换为液态玻璃语言，保证交互层和主视觉风格一致。

## 2026-05-27 12:24 +08:00 | Codex

- 参考用户指定的 iOS 风格液态玻璃方向，继续增强玻璃层的高光边缘、底部反射和内部折射层。
- 将导航条与首屏局部玻璃块改得更薄、更亮、更像连续流体材质，进一步减少“半透明面板叠加”的既视感。

## 2026-05-27 12:34 +08:00 | Codex

- 根据页面批注进一步精简 hero：删除顶部英文 badge，头像恢复圆形，标题与描述区移除文字托底框，仅保留按钮液态玻璃。
- 将标题颜色调整为首字 `Q` 保持粉色、`AQ` 改为白色，并重新收紧首屏排版，减少中心遮挡。

## 2026-05-27 12:46 +08:00 | Codex

- 针对用户指出“液态玻璃实现依旧不理想”，补充调研现成实现，参考 `rdev/liquid-glass-react` 与 `shuding/liquid-glass` 的 SVG displacement 思路。
- 在布局层接入全局 `liquid-glass-distortion` SVG filter，并将当前液态玻璃样式改为带真实 backdrop distortion 的实现，不再停留在纯 CSS 高光模拟。

## 2026-05-27 13:02 +08:00 | Codex

- 复查线上效果后确认之前版本仍偏“高光磨砂”，导航层的流动感和折射感不明显。
- 新增独立 `LiquidGlass` 组件，将导航外框、导航胶囊和右侧按钮改为组件化液态玻璃实现，加入独立 turbulence / displacement / sheen / drift 动画层。

## 2026-05-27 13:12 +08:00 | Codex

- 再次复查后确认仅靠 `backdrop-filter + displacementMap` 的感知强度仍不足，肉眼更像轻度磨砂。
- 将 `LiquidGlass` 调整为四层结构：backdrop / distortion / highlight / caustic，强化持续位移动画和表面流动高光，让导航内部折射变化更可见。

## 2026-05-27 14:09 +08:00 | Codex

- 用户确认接受“重构导航液态玻璃”的路线后，放弃导航内多个独立玻璃件的并列结构，改为一整块连续主液态容器。
- 将中间导航、搜索和 GitHub 从“多个小玻璃组件”回收为主容器内的分区元素，为下一步做真正的一体式流体导航打基础。

## 2026-05-27 14:18 +08:00 | Codex

- 根据用户要求将主导航容器从偏方形重新收回圆胶囊外形。
- 为中间导航分区补充一层连续的内部液态底板，减少“外壳是圆的，内部是散件”的割裂感。

## 2026-05-27 14:32 +08:00 | Codex

- 用户明确要求回退液态玻璃与悬浮导航方案，改为常规博客顶部通栏。
- 移除导航相关的液态玻璃组件、SVG filter 注入和对应样式，改回贯穿顶部的标准横条式导航，并保留站点原有粉紫配色。

## 2026-05-27 14:39 +08:00 | Codex

- 根据页面批注删除文章区标题上方的 `LATEST NOTES` 小标签，仅保留“最新文章”主标题与说明文字。

## 2026-05-27 14:44 +08:00 | Codex

- 根据页面批注将文章区整体继续下移，避免文章区标题和说明侵入 hero 收尾区域。
- 同时收紧 hero 与文章区之间的过渡遮罩位置，让首屏结束和正文开始的分界更清楚。

## 2026-05-27 14:50 +08:00 | Codex

- 根据页面批注将背景图可见范围收回到 hero 区，不再让图片尾部延伸到文章区上方。
- 通过降低顶部背景层高度并加深底部收口，让文章区从纯页面背景开始，而不是继续承接 hero 图片。

## 2026-05-27 15:00 +08:00 | Codex

- 根据页面批注重新调整 hero 内容块的位置和布局，将桌面端内容从正中央压图位置改为偏左落点。
- 同时放宽标题与摘要层级节奏，让 avatar、标题、摘要、标签和按钮的阅读顺序更清楚，减少对人物主体的遮挡。

## 2026-05-27 15:08 +08:00 | Codex

- 根据页面批注调整顶部静止态导航外观，使其在页面顶部时呈现更轻的半透明效果。
- 保留滚动后的实色可读性状态，仅增强顶部静止时的通透感与细微内高光。

## 2026-05-27 15:16 +08:00 | Codex

- 根据页面批注重新规划 hero 布局，将 avatar 提升为最上方居中的视觉锚点。
- 将 hero 内容块收回中轴纵向结构，使标题、描述、标签和按钮围绕同一中心线展开，减少布局分散感。

## 2026-05-27 15:24 +08:00 | Codex

- 参考用户给出的卡片风格方向，重构首页文章卡片的面板层次，强化顶部信息带、轮廓、阴影与 hover 反馈。
- 调整无封面卡片和有封面卡片的一致性，使文章区整体更像成组展示的信息卡，而不是普通深色块列表。

## 2026-05-27 15:31 +08:00 | Codex

- 继续向用户提供的卡片参考靠拢，增强卡片外轮廓、高光边和 hover 时的旋转感边框扫光。
- 在保留当前站点粉紫语义的前提下，让卡片更像独立组件化面板，而不是仅有轻微发光的内容容器。

## 2026-05-27 15:38 +08:00 | Codex

- 根据用户要求保留现有光效，但将卡片主体从厚重深色面板改为更通透的 2.5D 曲面玻璃质感。
- 通过更强的顶部高光、半透明层叠和更轻的底色，提升卡片的玻璃感，同时保留紫粉发光边缘与扫光动效。

## 2026-05-27 15:45 +08:00 | Codex

- 继续加强卡片的 2.5D 曲面体积感，在卡片顶部和底部增加更明确的弧面高光与内反射层。
- 进一步降低主体厚重感，让卡片更接近“透亮曲面玻璃”而非深色发光面板。

## 2026-05-27 15:53 +08:00 | Codex

- 根据用户要求为文章区背景增加伪星空和随机浮动粒子特效，用多层星点、柔光云团和缓慢漂浮的发光粒子打散原本整片黑底。
- 控制装饰强度在卡片内容之下，保证背景有氛围但不干扰文章阅读。

## 2026-05-27 16:00 +08:00 | Codex

- 用户反馈文章区背景装饰不够明显后，显著提高了星点密度、冷暖光团对比和粒子亮度。
- 额外加入若隐若现的细线与圆环装饰，增强文章区“不是整片黑底”的识别度，同时继续压在内容层之下。

## 2026-05-27 16:08 +08:00 | Codex

- 根据用户要求撤回对文章区大底色的改动，不再通过整体蓝紫蒙层改变原始底色。
- 保留星点、粒子、细线、圆环和柔光云团等装饰层，只通过附加装饰增强氛围而不改变底色基调。

## 2026-05-27 16:14 +08:00 | Codex

- 用户继续要求增强文章区背景装饰后，新增更大更亮的十字星、漂浮轨道环与椭圆虚线轨迹。
- 将装饰从“星点和粒子”扩展为“星点 + 粒子 + 轨道 + 闪烁星芒”的多层组合，让背景氛围更容易被直接感知。

## 2026-05-27 16:15 +08:00 | Codex

- 根据用户要求继续提高文章卡片的透明度与通透感，降低深色面板的压迫感。
- 增强卡片顶部高光、边缘透亮度与正文区透感，让整体更接近轻盈玻璃而不是厚重塑料板。

## 2026-05-27 16:22 +08:00 | Codex

- 根据用户进一步要求，将文章卡片主体压到接近完全透明，仅保留玻璃边缘、高光层和轻微折色。
- 同步降低标签底板与主体底板的不透明度，让整张卡片看起来更像悬浮玻璃而不是深色容器。

## 2026-05-27 16:29 +08:00 | Codex

- 继续压低文章卡片主体存在感，将主体底板进一步推向近乎完全透明，仅依赖边缘、高光和曲面反射维持形体。
- 同时减弱分隔线和底部压暗，使卡片更像漂浮玻璃片而不是仍带底色的面板。

## 2026-05-27 16:36 +08:00 | Codex

- 在接近完全透明的基础上继续削弱卡片主体白雾、底部反射和边框阴影，让玻璃形体更多依赖边缘与顶部高光成立。
- 进一步减小标签底板存在感，避免局部半透明块重新把卡片读成实体容器。

## 2026-05-27 16:43 +08:00 | Codex

- 复查当前实现后确认主要问题不是主体底色，而是叠加的多层高光和径向雾层过强，导致视觉上仍像实体面板。
- 继续削弱整卡顶部白雾、径向高光和阴影强度，只保留最轻的边缘高光和折色提示。

## 2026-05-27 16:50 +08:00 | Codex

- 继续按“去底板感”方向收窄卡片的上缘高光、径向泛光和内层反射，只保留极轻的边界提示。
- 再次降低标签底板与分隔线存在感，避免任何局部层仍把卡片读成实体板块。

## 2026-05-27 16:57 +08:00 | Codex

- 继续削减卡片的顶部横向白雾、边框内光和 hover 阴影，让主体更少被读成实体板块。
- 将卡片进一步推向“只有描边和极轻反射成立”的透明玻璃片状态。

## 2026-05-27 17:11 +08:00 | Codex

- 由于用户持续反馈“卡片并不透明”，放弃在原卡片上继续微调，直接重绘文章卡片组件。
- 新版本卡片改为几乎纯透明底，仅保留极轻边框、顶部细高光、微弱折色和文字层，用来彻底排除残留蒙版/厚重面层问题。

## 2026-05-27 17:18 +08:00 | Codex

- 在重绘版本基础上继续做更激进的“去面层”处理，将整面高光和底板感进一步压掉。
- 当前版本只保留极细描边、极轻折色、窄顶边亮线与文字层，最大化验证卡片透明状态。

## 2026-05-27 17:26 +08:00 | Codex

- 复核线上版本确认已部署后，判断问题不在“有没有发版”，而在“完整卡形本身仍然会被读成实体容器”。
- 因此再次重绘文章卡片，进一步弱化完整卡形，只保留极细轮廓、极弱顶部亮线与信息层，尽量把视觉重点从“面板”转成“浮在背景上的内容”。

## 2026-05-27 19:25 +08:00 | Codex

- 根据用户要求，将搜索页和归档页补齐为与文章页一致的背景语言。
- 为搜索页和归档页加入与文章页同风格的固定背景图、渐变遮罩与柔光氛围层，保证各主要页面视觉基调统一。

## 2026-05-27 19:34 +08:00 | Codex

- 用户切换到文章页移动端适配需求后，优先优化代码块在窄屏下的字体、内边距、滚动与容器边距。
- 同时收窄文章页头部与正文容器的小屏内边距，避免移动端代码块在视觉上显得过挤或溢出。

## 2026-05-27 17:11 +08:00 | Codex

- 由于用户持续反馈“卡片并不透明”，放弃在原卡片上继续微调，直接重绘文章卡片组件。
- 新版本卡片改为几乎纯透明底，仅保留极轻边框、顶部细高光、微弱折色和文字层，用来彻底排除残留蒙版/厚重面层问题。

## 2026-05-29 14:24 +08:00 | Codex

- 删除测试文章 `content/markdown/markdown-showcase.md`，避免其继续出现在正式站点的文章列表与归档统计中。
- 重新生成 `lib/blog-data.ts`，确认博客数据中已移除 `markdown-showcase`，归档统计同步回落为真实文章数量。
- 在 NTFS 临时副本中完成 `npm ci && npm run build` 验证，确认删除后站点仍可正常构建。

## 2026-05-29 22:09 +08:00 | Codex

- 新增浅色主题预览页 `/light-theme-preview/`，仅作为本地预览入口，不影响当前正式暗色主题和导航。
- 将用户提供的浅色背景图压缩为 `public/light-theme-bg.webp`，用于预览页固定背景，避免直接引入 51MB 原图。
- 为浅色预览新增独立作用域样式 `.light-preview`，重做浅色卡片、正文、代码块、引用块、表格与滚动条配色。
- 根据预览反馈将卡片配色收敛为更干净的白色/蓝灰体系，并将首屏标题改为亮蓝 `Q` + 白色 `AQ`。
- 在 NTFS 临时副本中完成 `npm ci && npm run build` 验证，确认新增预览页可正常静态生成。

## 2026-05-29 23:29 +08:00 | Codex

- 修正浅色主题引用块配色，将便签底色、左侧强调线、爪印装饰和正文文字统一为亮蓝/蓝灰体系，避免残留暗色/粉色观感。
- 将浅色预览方案接入真实博客，新增 Header 深浅切换入口，并覆盖首页、搜索页、归档页、文章页、页脚、文章卡片、正文代码块与引用块的浅色样式。
- 使用用户提供背景图压缩产物 `public/light-theme-bg.webp` 作为浅色主题背景，并补齐首页首屏与搜索/归档/文章页背景层切换。
- 修复本地 `127.0.0.1:3000` 黑屏问题：允许 Next dev 资源来源 `127.0.0.1`，并调整页面过渡/关键卡片透明度兜底，避免客户端动画未接管时停留在透明初始态。
- 本地通过浏览器验证首页、搜索页、归档页与文章页浅色切换；在 NTFS 临时副本中完成 `npm ci && npm run build` 验证。

## 2026-05-29 23:41 +08:00 | Codex

- 根据浏览器批注继续修正浅色首页首屏，将 `HeroSection` 内部独立暗色幕布层标记为 `.hero-curtain`。
- 为 `.light .hero-curtain` 增加浅色柔光渐变覆盖，避免浅色模式下首屏仍被深色幕布压暗。
- 本地浏览器验证 `.hero-curtain`、首页背景图与外层渐变均已切到浅色体系；在 NTFS 临时副本中完成 `npm ci && npm run build` 验证。

## 2026-05-30 00:01 +08:00 | Codex

- 根据浏览器反馈继续清理浅色模式残留旧暗色/紫粉炫光，为文章卡片、关于卡片、文章正文卡片、首页头像与页脚装饰层补充独立类名。
- 在浅色主题下将卡片扫光、径向柔光、图片遮罩、头像光晕、页脚高光统一替换为蓝白/蓝灰体系，避免卡片边缘和 hover 状态出现暗色模式观感。
- 使用内置浏览器验证首页、搜索页与文章页浅色模式，旧粉紫/暗色光效计算样式扫描结果为 0；在 NTFS 临时副本中完成 `npm ci && npm run build` 验证。

## 2026-05-30 00:05 +08:00 | Codex

- 根据浏览器批注关闭浅色关于大卡 hover 时的淡化/洗白效果，禁用该卡片在浅色模式下的 hover 位移、边框变化和扫光层。
- 保留浅色蓝白柔光底色，但将 about 大卡的 hover 光效固定为静态状态，避免鼠标悬停时卡片整体变淡。

## 2026-05-30 00:10 +08:00 | Codex

- 复查浅色关于大卡 hover 变淡问题，确认通用 .group:hover .card-sweep-wrap 规则在后置覆盖中重新打开了扫光层。
- 将 about 大卡从 group hover:-translate-y 交互中移出，并提高浅色 about 专用禁用规则优先级，彻底关闭该卡片 hover 洗白效果。

## 2026-05-30 00:13 +08:00 | Codex

- 根据用户要求参考深色模式卡片渲染，恢复浅色 about 大卡的 group hover 结构和轻微上浮交互。
- 将浅色 about 的 hover 扫光改为低透明度蓝白扫光，并保留静态蓝白径向柔光，避免先前强扫光导致整卡变淡。

## 2026-05-30 00:15 +08:00 | Codex

- 针对浅色 about 大卡 hover 仍变淡的问题，保留深色同款上浮、边框与阴影反馈，但取消 hover 时的柔光增亮。
- 将浅色 about 大卡的扫光层 hover 透明度固定为 0，并将径向柔光固定为低透明度，

## 2026-05-30 00:23 +08:00 | Codex

- 根据用户提供的 hover/正常态对比图继续修正浅色 about 大卡：父卡 hover 时不再改变整体背景、阴影、位移和文字颜色。
- 为 about 标题、描述、小卡标签、小卡正文和小卡背景补充 hover 锁定样式，使父卡悬停时保持正常态观感，仅保留子小卡自身交互。

## 2026-05-30 00:32 +08:00 | Codex

- 根据用户要求恢复浅色 about 大卡的 hover 位移反馈，主卡与右侧作者卡重新使用 `transition-all` 与轻微上浮交互。
- 将浅色 about 大卡扫光层从完全禁用改为低透明度蓝白扫光，同时保留标题、描述、小卡文字与背景的 hover 锁定，避免再次出现整卡变淡。

## 2026-05-30 00:35 +08:00 | Codex

- 针对恢复扫光后浅色 about 大卡再次变淡的问题，将扫光层改为仅作用于 1px 边框区域的掠光遮罩。
- 保留父卡轻微上浮反馈，但避免扫光覆盖正文与小卡内容区域，减少 hover 时整卡洗白。

## 2026-05-30 00:44 +08:00 | Codex

- 根据用户反馈放弃边框遮罩扫光方案，浅色 about 大卡重新复用深色模式原本的旋转扫光层级。
- 浅色下仅将扫光改为低透明度蓝色并使用 multiply 混合，避免白色光芯覆盖卡面导致 hover 发白。

## 2026-05-30 01:01 +08:00 | Codex

- 实现深浅主题切换的星尘扫场动画，主题按钮会在切换前派发 qaq-theme-transition 事件并触发全局覆盖层。
- 将首页、搜索页、归档页与文章页背景改为暗/浅双图层交叉淡入，避免直接替换背景图造成闪烁或突兀放大。
- 为主题切换背景层补充 opacity、filter、scale 过渡，并在 prefers-reduced-motion 下关闭星尘扫场，仅保留短淡入淡出。
- 使用内置浏览器验证首页、搜索页、归档页与文章页背景层；在 NTFS 临时副本中完成 `npm ci && npm run build` 验证。

## 2026-05-30 01:17 +08:00 | Codex

- 将浅色主题背景替换为用户提供的 Y2K 守护甜心壁纸，并压缩覆盖 `public/light-theme-bg.webp`。
- 将浅色主题变量、正文、卡片、Header、Hero、页脚、代码块、引用块、星尘切换动画与浅色预览页统一迁移为粉紫/奶白/黑描边感色系。
- 修复 `PageBackground` 对无尾斜杠路径的匹配，确保搜索页、归档页和文章页浅色背景图层稳定显示。
- 使用内置浏览器验证首页、搜索页、归档页和文章页浅色图层与文字色；在 NTFS 临时副本中完成 `npm ci && npm run build` 验证。

## 2026-05-30 01:30 +08:00 | Codex

- 根据首页浏览器批注，将浅色首页首屏 Hero 幕布从米黄兜底调整为粉白渐变，包含 `hero-curtain`、`home-hero-shade` 与全局浅色 `body` 底色。
- 同步清理正式浅色主题中 about 过渡底、文章卡片图遮罩和 footer 的米黄残留，使首屏底部与后续区域保持粉白系。
- 拆分 Header Logo 的 `Q` 与 `AQ` 文本节点，并将浅色模式 Logo 调整为粉色 `Q` + 莓紫 `AQ`，提升玻璃 Header 上的可读性。
- 使用内置浏览器读取计算样式验证：首页浅色底色为 `rgb(255, 246, 251)`，Hero 遮罩与 Logo 色值已切换到粉白/莓紫体系；执行 `git diff --check` 通过。

## 2026-05-30 01:40 +08:00 | Codex

- 根据首页文章区批注，为浅色主题新增“糖霜亮片 + 少量贴纸”动画层，使用粉白丝带、菱形亮片、十字星与小贴纸替代不明显的暗色星空。
- 保留深色主题原有星点与流星表现，仅在 `.light #posts` 下隐藏旧星空/流星并显示浅色专用装饰层，避免影响暗色主题。
- 使用内置浏览器验证浅色文章区：新装饰层 opacity 为 1，亮片数量 8、贴纸数量 3，旧流星与旧星点 opacity 为 0；执行 `git diff --check` 通过。

## 2026-05-30 01:57 +08:00 | Codex

- 优化首页跳转到搜索页/归档页的背景接力动画，将 `PageBackground` 改为常驻待命层，首页时隐藏、目标页或预热态时渐入，避免从 `null` 挂载导致硬切。
- 为首页点击搜索/归档入口增加 180ms 背景预热：点击时先显示目标页背景与一次性柔光 wash，再执行路由跳转，使首页背景与子页面背景自然衔接。
- 为搜索/归档背景补充路由过渡状态样式，背景层在跳转中轻微缩放，柔光 wash 使用 keyframe 自动退场，避免动画尾巴残留。
- 使用内置浏览器验证首页到搜索/归档的深色与浅色路径：跳转中目标背景层 opacity 渐升，落页后背景稳定、wash opacity 回到 0；执行 `git diff --check` 通过。

## 2026-05-30 02:03 +08:00 | Codex

- 根据搜索页反馈柔化浅色切换到深色时的主题扫场高光，将深色方向的星尘核心从近白/亮粉改为低亮度夜紫，并降低 spark 与 glow 透明度。
- 为 `.theme-transition-overlay--dark` 单独改用 `soft-light` 混合、增大 blur、缩小 spark 尺寸，并新增深色专用 `theme-stardust-sweep-dark` 关键帧，减少峰值亮度。
- 使用内置浏览器在 `/search/` 验证浅色切换到深色：深色 overlay 的 sweep 混合模式为 `soft-light`，峰值 opacity 约 0.34，动画结束后 overlay class 正常清空；执行 `git diff --check` 通过。

## 2026-05-30 03:18 +08:00 | Codex

- 根据用户澄清，修正目标为“首页跳转到搜索/归档页”的路由背景过渡，而非深浅主题切换动画。
- 调整 `PageBackground` 的预热逻辑：仅在首页点击 `/search/` 或 `/archive/` 时触发一次过渡，并改为 pointer/click 预热但不 `preventDefault`，避免接管 Next 自身导航。
- 将路由过渡从带方向感的亮带扫场改为低对比暗场接力：压低背景亮度、降低径向柔光，并用轻量像素网格 mask 做短暂衔接，避免出现突兀高光。
- 使用内置浏览器验证首页到搜索页、首页到归档页的浅色路径：旧 `.page-bg-route-wash` 数量为 0，链接可正常跳转；执行 `git diff --check` 通过，`npx tsc --noEmit` 仍受既有 Framer Motion `Variants` 类型问题阻塞。

## 2026-05-30 03:38 +08:00 | Codex

- 新增友链页面 `/friends/`，使用与搜索页/归档页一致的全局背景层，并提供友链标题区、卡片网格和友链申请信息区。
- 友链卡片复用文章预览卡片的边框、柔光、扫光与 hover 语言，并新增头像展示区、状态徽标、标签和访问入口。
- 将友链入口接入 Header 与 Footer，并扩展首页跳转预热背景逻辑，使 `/friends/` 与搜索/归档页面背景过渡一致。
- 为友链头像区补充深色/浅色专属样式；同时为公共 Framer Motion 变体补充类型标注，修复类型检查中已有的 `Variants` 推断问题。
- 本地通过内置浏览器验证 `/friends/` 页面 DOM 与背景状态，执行 `npx tsc --noEmit --pretty false` 与 `git diff --check` 均通过。

## 2026-05-30 14:05 +08:00 | Codex

- 将用户提供的 PaperPlane 头像复制到 `public/friends/paperplane.jpg`，并接入友链页头像区。
- 将第一条友链替换为真实站点 `PaperPlane`：链接 `http://blog.paperplane.codes/`，简介 `RE神！`，标签调整为逆向/安全方向。
- 根据反馈删除友链页剩余 mock/占位卡片，目前友链列表仅保留 PaperPlane 一张真实卡片。
- 使用内置浏览器验证 `/friends/`：卡片数量为 1、头像路径正确、mock 文案已不存在；执行 `npx tsc --noEmit --pretty false` 与 `git diff --check` 均通过。

## 2026-05-30 14:09 +08:00 | Codex

- 将用户提供的 Fa1lSnow 头像复制到 `public/friends/fa1lsnow.jpg`，并接入友链页头像区。
- 新增第二条真实友链 `Fa1lSnow`：链接 `https://blog.fa1lsnow.com/`，简介 `PWN神！`，标签调整为 Pwn/Binary/Security。
- 使用内置浏览器验证 `/friends/`：当前友链卡片数量为 2，PaperPlane 与 Fa1lSnow 外链、头像路径与加载尺寸均正确。
- 执行 `npx tsc --noEmit --pretty false` 与 `git diff --check` 均通过。

## 2026-05-30 21:46 +08:00 | Codex

- 部署前清理本地 `tsconfig.tsbuildinfo` 临时产物，并执行 `git diff --check` 与 `npx tsc --noEmit --pretty false`，均通过。
- 由于当前工作目录的 Next webpack 构建在 Windows 上触发 `readlink` 缓存异常，改用 NTFS 临时干净副本执行 `npm ci && npm run build` 验证。
- 临时副本构建成功，确认新增 `/friends` 静态页面、友链头像资源与现有文章/搜索/归档页面均可正常生成。
- 准备将本轮深浅主题、背景动效、友链页面与两条真实友链改动提交并推送到 `origin/main`。
## 2026-05-30 23:56 +08:00 | Codex

- 调研博客右下角互动角色方案后，先落地轻量“伪 Live2D”挂件，避免直接引入完整 Live2D SDK 与模型制作流程带来的体积和接入复杂度。
- 将用户提供的人物图处理为透明 WebP 资源 `public/widgets/purple-companion.webp`，并新增全局 `InteractiveCompanion` 客户端组件。
- 挂件支持气泡文案轮播、点击回应、hover 情绪、拖拽、最小化与关闭/唤回，深色与浅色主题下分别使用紫粉/粉白玻璃质感。
- 接入 `components/providers.tsx`，使首页、搜索、归档、文章与友链等页面都能显示同一个互动角色入口。
- 使用内置浏览器验证本地首页挂件 DOM、图片加载与最小化/恢复流程；执行 `npx tsc --noEmit --pretty false` 与 `git diff --check` 均通过。
## 2026-05-31 00:20 +08:00 | Codex

- 针对“只是图片上下晃、眼睛嘴巴错位”的反馈，重构互动角色为更保守的类 Live2D 分层动效。
- 移除手工绘制的眼睛与嘴巴覆盖层，避免五官坐标偏移导致出戏；当前保留原图五官，仅对整体、头发、外套与耳饰做微弱视差/呼吸/摆动。
- 将鼠标跟随变量接入挂件 stage，通过 `--look-x` / `--look-y` 驱动分层轻微旋转与位移，同时保留点击、hover、拖拽、最小化和关闭/唤回能力。
- 重新处理挂件素材为上半身透明 WebP，降低背景残留对右下角挂件的影响；`rembg` 模型下载因 GitHub release asset 超时未能完成，暂未采用 AI 分割结果。
- 使用内置浏览器验证本地首页挂件 DOM：分层节点为 4，眼睛/嘴巴覆盖层为 0；执行 `npx tsc --noEmit --pretty false` 与 `git diff --check` 均通过。

## 2026-05-31 01:32 +08:00 | Codex

- 改用用户已扣好的透明素材 `C:/Users/QAQ/Downloads/live_2d/test.png`，重新生成更干净、更轻量的右下角角色 WebP 主图。
- 从同一素材额外导出真实透明分层：后发、前发、外套与饰品层，替代运行时 CSS 裁切，减少旧素材残留与脸部重影。
- 保持原图五官完整，不再叠加任何手绘眼睛或嘴巴；类 Live2D 动效仅作用于整体姿态、发丝、外套、饰品和周围光晕。
- 调整角色舞台比例、拖拽边界、鼠标跟随中心与气泡位置，使半身角色完整显示，气泡不再遮挡人物脸部。
- 使用内置浏览器验证本地首页挂件 DOM：分层节点为 5，眼睛/嘴巴覆盖层为 0；执行 `npx tsc --noEmit --pretty false` 与 `git diff --check` 均通过。

## 2026-05-31 01:46 +08:00 | Codex

- 为右下角互动角色新增安全表情层，继续保留原图五官，不叠加错位的手绘眼睛或嘴巴。
- 根据 `idle`、`curious`、`happy` 三种状态切换表情：待机显示轻微眼部高光，好奇显示问号与小汗滴，点击开心显示腮红与爱心。
- 表情层跟随 `--look-x` / `--look-y` 做极轻微位移，并接入降动效媒体查询，避免过度闪烁或遮挡角色脸部。
- 使用内置浏览器验证：表情节点数量为 6，点击后进入 `is-happy`，腮红/爱心可见，`.companion-eye` 与 `.companion-mouth` 覆盖层仍为 0。
- 执行 `git diff --check` 与 `npx tsc --noEmit --pretty false`，均通过。

## 2026-05-31 02:02 +08:00 | Codex

- 在友链页新增第三条真实友链 `DUSK`，地址补齐为 `https://fallingdusky.github.io/`。
- 接入远程头像 `https://fallingdusky.github.io/avatar/avatar.webp`，简介为 `Fa1lSnow 亲传大弟子`，并按现有卡片字段补充分类、状态、缩写和标签。
- 使用内置浏览器验证 `/friends/`：当前友链卡片数量为 3，DUSK 外链、头像地址与简介均正确显示。
- 执行 `git diff --check` 与 `npx tsc --noEmit --pretty false`，均通过。

## 2026-06-01 20:35 +08:00 | Codex

- 根据用户“不保留当前 WebP 分层”的要求，移除右下角互动角色的伪 Live2D/WebP 分层实现，改为真实 Live2D Canvas 渲染。
- 将 `C:/Users/QAQ/Downloads/LIVE2D` 中的 Cubism 模型整理到 `public/live2d/dusk-companion/`，并统一命名为 `model.model3.json`、`model.moc3`、`model.cdi3.json` 与 `textures/texture_00.png`。
- 检查 `.moc3` 头部为 `MOC3 06`，确认旧 `pixi-live2d-display` 不兼容该 Cubism 6 模型；卸载 `pixi.js` 与 `pixi-live2d-display`，改用支持 Cubism 6 的 `l2d`。
- 重写 `InteractiveCompanion`：保留气泡、拖拽、最小化、关闭/唤回、hover 与点击情绪状态，同时通过 `l2d.setParams` 驱动朝向、眼球、眉毛和嘴型等模型参数。
- 保留现有暗色/浅色挂件玻璃 UI 与光效样式，但不再加载 `/widgets/` 资源，也不再叠加手绘表情层，避免五官错位和旧素材残留。
- 使用内置浏览器验证本地首页：`.interactive-companion` 状态为 `is-ready`，右下角真实模型可见，旧 `.companion-part` / `.companion-expression` / `/widgets/` 节点数量为 0。
- 执行 `git diff --check` 与 `npx tsc --noEmit --pretty false` 均通过；当前目录 `npm run build` 仍触发既有 Windows `readlink` 异常，已在 NTFS 临时副本执行 `npm ci && npm run build` 并通过。

## 2026-06-01 21:04 +08:00 | Codex

- 在友链页新增第四条真实友链 `Po1nt`，地址补齐为 `https://po1nttt.github.io/`。
- 接入 GitHub 远程头像 `https://avatars.githubusercontent.com/u/241124566?v=4`，简介为 `大黑客！`，并按现有友链卡片字段补充分类、状态、缩写与标签。

## 2026-06-01 22:51 +08:00 | Codex

- 修复远端 Live2D 挂件生产环境报错 `Live2DCubismCore is not defined`：不再通过 Next 生产 chunk 动态导入 `l2d` ESM 包。
- 将 `l2d` 浏览器全局构建复制到 `public/live2d/runtime/l2d.min.js`，由 `InteractiveCompanion` 在客户端注入脚本并通过 `window.L2D.init(canvas)` 初始化模型。
- 保持用户要求的真实 Live2D Canvas 渲染路径，不恢复旧 WebP 分层或伪 Live2D 资源。
- 执行 `git diff --check` 与 `npx tsc --noEmit --pretty false`，均通过；当前目录 `npm run build` 仍触发既有 Windows `readlink` 异常，已在 NTFS 临时干净副本执行 `npm ci && npm run build` 并通过。
